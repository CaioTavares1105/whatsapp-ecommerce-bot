# ===========================================================
# .env.example - Template de Variáveis de Ambiente
# ===========================================================
# IMPORTANTE: Este arquivo é um TEMPLATE. Copie para .env e
# preencha com seus valores reais.
#
# NUNCA commite o arquivo .env no Git!
# Ele contém senhas e tokens que devem ser secretos.
#
# Para usar:
#   1. Copie: cp .env.example .env
#   2. Edite: abra .env e preencha os valores
# ===========================================================

# ----- APLICAÇÃO -----
# Nome da aplicação (aparece nos logs)
APP_NAME=whatsapp-ecommerce-bot

# Ambiente: development, staging ou production
APP_ENV=development

# Modo debug (true = mais logs, false = produção)
DEBUG=true

# Chave secreta para criptografia (JWT, sessões)
# IMPORTANTE: Em produção, use uma chave forte e única!
# Gerar: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=sua-chave-secreta-aqui-mude-em-producao

# ----- BANCO DE DADOS (PostgreSQL) -----
# Formato: postgresql+asyncpg://usuario:senha@host:porta/banco
# asyncpg = driver assíncrono para PostgreSQL
DATABASE_URL=postgresql+asyncpg://postgres:senha123@localhost:5432/chatbot_db

# ----- CACHE (Redis) -----
# Formato: redis://host:porta/numero_do_banco
# Redis é usado para cache e armazenamento de sessões
REDIS_URL=redis://localhost:6379/0

# ----- WHATSAPP CLOUD API (Oficial da Meta) -----
# Obter em: https://developers.facebook.com/docs/whatsapp/cloud-api
# 
# Token de acesso à API do WhatsApp
WHATSAPP_API_TOKEN=seu-token-aqui

# ID do número de telefone configurado no WhatsApp Business
WHATSAPP_PHONE_NUMBER_ID=seu-phone-id

# Token de verificação (você escolhe, usado no webhook)
WHATSAPP_VERIFY_TOKEN=seu-verify-token

# Segredo do webhook (para validar requisições)
WHATSAPP_WEBHOOK_SECRET=seu-webhook-secret

# ----- API -----
# Host onde a API vai rodar (0.0.0.0 = todas interfaces)
API_HOST=0.0.0.0

# Porta da API
API_PORT=8000

# ----- LOGGING -----
# Nível de log: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
